from setuptools import setup, find_packages

setup(
    name="sureshot-automation",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add dependencies as needed
    ],
    python_requires=">=3.8",
)