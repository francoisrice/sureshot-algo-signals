# Automation

## How this folder works

The head .yaml is the K8s deployment to manage each strategy. Each strategy is a container running in the deployment on a schedule. Each strategy gets executed in time.

Note: build containers from automation/
