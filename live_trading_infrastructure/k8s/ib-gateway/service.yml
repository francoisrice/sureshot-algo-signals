apiVersion: v1
kind: Service
metadata:
  name: ib-gateway-service
  namespace: trading
spec:
  selector:
    app: ib-gateway
  ports:
  - name: gateway
    port: 5000
    targetPort: 5000
  type: ClusterIP
